# Dev config — loaded by default (no -Dconfig.resource override needed).
app {
  host = "0.0.0.0"
  port = 8080
  sqlite-path = "data/cask.sqlite"
  auth {
    session-cookie-name = "cask_session"
    secure-cookie = false
    google-dev-login-enabled = true
    session-ttl-days = 7
  }
}
