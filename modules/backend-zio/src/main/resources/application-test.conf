# Test config — select with -Dconfig.resource=application-test.conf
# Separate port and sqlite file from dev so a test run never touches dev data.
app {
  host = "127.0.0.1"
  port = 18081
  sqlite-path = "data/zio-test.sqlite"
}
